<nav class="site-nav">
    <div class="site-nav-left">
        {{#if @site.logo}}
            <a class="site-nav-logo" href="{{@site.url}}"><img src="{{@site.logo}}" alt="{{@site.title}}" /></a>
        {{else}}
            <a class="site-nav-logo" href="{{@site.url}}">{{@site.title}}</a>
        {{/if}}
        <div class="site-nav-content">
            {{#if @site.navigation}}
                {{navigation}}
            {{/if}}
            {{#is "post"}}
                <span class="nav-post-title {{#unless @site.logo}}dash{{/unless}}">{{post.title}}</span>
            {{/is}}
        </div>
    </div>
    <div class="site-nav-right">
        <div class="social-links">
            {{#if @site.facebook}}
                <a class="social-link social-link-fb" href="{{facebook_url @site.facebook}}" title="Facebook" target="_blank" rel="noopener">{{> "icons/facebook"}}</a>
            {{/if}}
            {{#if @site.twitter}}
                <a class="social-link social-link-tw" href="{{twitter_url @site.twitter}}" title="Twitter" target="_blank" rel="noopener">{{> "icons/twitter"}}</a>
            {{/if}}
            {{#if @config.theme.sns.weibo}}
                <a class="social-link social-link-tw" href="{{@config.theme.sns.weibo}}" title="Weibo" target="_blank" rel="noopener">{{> "icons/weibo"}}</a>
            {{/if}}
            {{#if @config.theme.sns.instagram}}
                <a class="social-link social-link-tw" href="{{@config.theme.sns.instagram}}" title="Instagram" target="_blank" rel="noopener">{{> "icons/instagram"}}</a>
            {{/if}}
            {{#if @config.theme.sns.github}}
                <a class="social-link social-link-tw" href="{{@config.theme.sns.github}}" title="Github" target="_blank" rel="noopener">{{> "icons/github"}}</a>
            {{/if}}
            {{#if @config.theme.sns.linkedin}}
                <a class="social-link social-link-tw" href="{{@config.theme.sns.linkedin}}" title="LinkedIn" target="_blank" rel="noopener">{{> "icons/linkedin"}}</a>
            {{/if}}
        </div>
        {{#if @labs.members}}
            <a class="subscribe-button" href="#subscribe">Subscribe</a>
        {{else}}
            <a class="rss-button" href="https://feedly.com/i/subscription/feed/{{@site.url}}/rss/" title="RSS" target="_blank" rel="noopener">{{> "icons/rss"}}</a>
        {{/if}}
    </div>
</nav>
